# Normalise line endings so an editor or script that writes CRLF does not turn a
# six-line change into a whole-file diff.
* text=auto eol=lf

# Binary-ish assets git should not touch
*.xlsx binary
*.gguf binary
*.png  binary
