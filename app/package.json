{
  "name": "fish-ai",
  "version": "1.1.0",
  "description": "Fish.AI web UI - static page plus a thin proxy. No dependencies; run from the repo root with start.ps1.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.mjs"
  }
}
